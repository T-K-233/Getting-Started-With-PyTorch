
dataloader:
  path: "data/names/*.txt"
  # path: "data/eng-fra.txt"

model:
  hidden_layer_size: 32
  # hidden_layer_size: 128
  path: ./logs/model.pth

trainer:
  learning_rate: 0.005
  # n_iters: 100000
  n_iters: 300000

logger:
  log_every: 10000